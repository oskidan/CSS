{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Unit Tests",
            "command": "./unitTests.sh && exit",
            "cwd": "${workspaceFolder:CSS}",
            "request": "launch"
        }
    ]
}